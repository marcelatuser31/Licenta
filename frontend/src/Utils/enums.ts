export enum RoleType {
    Client = 'Client', Admin = 'Admin'
}

export enum Pages {
    Home = "/Home",
    LogIn = "/LogIn",
    Register = "/Register",
    Cake = "/Cake",
    ShoppingCart = "/ShoppingCart"
}